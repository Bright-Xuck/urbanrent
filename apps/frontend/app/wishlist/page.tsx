import { InfoPage } from "../../components/ReferencePage";
export default function WishlistPage(){return <InfoPage title="Wishlist" eyebrow="SAVED PROPERTIES" intro="Keep your favorite properties together and revisit them whenever you are ready." links={[{label:"Explore Properties",href:"/properties"}]}/>}
