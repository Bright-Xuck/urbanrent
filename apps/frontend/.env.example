# Base URL of the backend API (must include /api).
# Local dev:
NEXT_PUBLIC_API_URL=http://localhost:5000/api
# Production (Render backend), set in Vercel:
# NEXT_PUBLIC_API_URL=https://urbanrent-api.onrender.com/api
